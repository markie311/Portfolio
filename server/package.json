{
  "dependencies": {
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "nodemailer": "^6.10.1"
  },
  "scripts": {
    "start": "node server.js",
    "nodemon": "nodemon server.js",
    "heroku-postbuild": "cd client && nnpm install && npm run build"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
